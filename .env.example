# =====================================================
# wohn-watch — environment configuration
# Copy to .env for local runs; paste into Coolify for deploy.
# =====================================================

# -- Telegram -------------------------------------------
# The only secret this bot needs. Create a bot with @BotFather.
TELEGRAM_BOT_TOKEN=

# -- Tuning ---------------------------------------------
# inberlinwohnen.de is read without an account — the public Wohnungsfinder
# carries the same listings and the same fields.

# How often to poll the Wohnungsfinder, in seconds.
SCRAPE_INTERVAL_SECONDS=60
# Listings older than this are pruned so the volume stays bounded.
FLAT_RETENTION_DAYS=30
# How long the per-message usage log is kept (the anonymous totals are kept
# forever). See "Nutzungslog" in the README.
EVENT_RETENTION_DAYS=90
# Where the SQLite database lives (a Docker volume in production).
DATA_DIR=/data
LOG_LEVEL=INFO
